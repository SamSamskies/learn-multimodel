# Lesson 5 complete — provider prefs and model fallbacks

User finished Lesson 5: reliability is two layers — provider failover (same model, default on via `allow_fallbacks`) vs opt-in `models[]` across model ids; audit who served with `GET /generation` → `provider_name`. Floor for next sessions: Fusion (multi-model deliberation when cost-of-wrong beats extra completions), then land the OpenRouter adapter in Inference Bridge.
