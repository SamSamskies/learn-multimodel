# Lesson 4 complete — live models catalog

User finished Lesson 4: `GET /api/v1/models` with server-side `sort` / filters (`max_price`, `supported_parameters`, `q`) is the Bridge picker data source; catalog `pricing` is per-token USD (×1e6 → $/M), distinct from billed `usage.cost`. Floor for next sessions: teach provider preferences and the two failover layers (provider vs `models` array) so reliability is configured, not hoped for.
