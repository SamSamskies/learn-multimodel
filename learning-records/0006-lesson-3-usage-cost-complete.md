# Lesson 3 complete — per-call cost and latency

User finished Lesson 3: every OpenRouter Chat Completions response includes `usage` (tokens + `cost`) by default; wall-clock latency is measured client-side. Floor for next sessions: measurement habit is in place — teach live catalog discovery (`GET /models`) so Bridge can populate a model picker from evidence, not a hardcoded slug list.
